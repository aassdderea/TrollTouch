name: build-trolltouch

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git $HOME/theos
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: Install ldid
        run: brew install ldid

      - name: Build dylib
        run: |
          export THEOS=$HOME/theos
          make clean package

      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: TrollTouch-dylib
          path: |
            packages/TrollTouch.dylib
            packages/entitlements.plist
            scripts/*.sh
            README.md
